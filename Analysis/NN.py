# This class feeds an event through the neural network

class NN():

   def analyzeEvent(alarmEvent):
      # TODO: write code to take an event and input each 
      # component to a neural network, then take action
      # based on the result
