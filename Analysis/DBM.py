# This class will create and update the Neural Network
# (hereafter referred to as the NN).

class DBM():
   def developClusters():
      # TODO: write code to take an input database 
      # containing motion and pass-through events 
      # and then perform a clustering algorithm on
      # the data, and finally output a DB/file
      # describing those clusters

   def developNN():
      # TODO: write code to take an input database
      # containing cluster info and then write a neural
      # network to perform classification of a new
      # entry into one of those clusters, then output
      # that neural network to a DB/file
